#include <bits/stdc++.h>
using namespace std;
int main() {
int t,x,y;
cin>>t;
while(t--) {
    cin>>x>>y;
    int startFrom = x+y;
    if(startFrom == 2)
    {
        cout<<"1"<<endl;
        continue;
        }
    else if(startFrom == 3)
        {cout<<"2"<<endl;
        continue;}

    for(int i = startFrom+1; i < INT_MAX;i++) {
        if(i%2 == 0)
            continue;
        else {
            int k = 2;
            while ( k < sqrt())
        }

    }
}


return 0;
    }
