#include <iostream>
using namespace std;

int keypadApproach(string s, string output[]) {
   if(s.empty())
     return 0;
   int digit = stoi(s)
 }

int main() {
	// your code goes here
    int n;
    int outputLen = 0;
    string *output = new string[outputLen];
    for(int i = 0 ; i < outputLen ; i++)
       cout<<keypadApproach(s,output)<<endl;
	return 0;
}